<div class= "exercise_category">
  <h5>運動カテゴリー一覧</h5>
  <table>
    <% @categories.each do |category| %>
      <tr>
        <td>
          <%= category.id %>.
          <%= category.name %>
        </td>
      </tr>
    <% end %>
  </table>
</div>
